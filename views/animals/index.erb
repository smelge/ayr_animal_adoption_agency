Main Page for pets

<table>
  <tr>
    <th>Image</th>
    <th>Name</th>
    <th>Species</th>
    <th>Age</th>
    <th>Adopt!</th>
  </tr>
  <% for pet in @pets %>
    <tr>
      <td><img src='/assets/<%= pet.photo %>' alt='<%= pet.species %>'/></td>
      <td><%= pet.name %></td>
      <td><%= pet.species %></td>
      <td><%= pet.age %></td>
      <td>#</td>
    </tr>
  <% end %>
</table>
